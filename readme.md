# Piano Midi

Este é um projeto desenvolvido com [Next.js](https://nextjs.org), inicializado via [`create-next-app`](https://nextjs.org/docs/app/api-reference/cli/create-next-app), e que utiliza scripts em **Python** para processamento.

---
### Como Iniciar

## Backend / Scripts (Python)
Siga as instruções abaixo para configurar o ambiente Python e rodar o script principal.

1. Criar o ambiente virtual
```bash
python -m venv venv
```

2. Ativar o ambiente virtual
- Linux e macOS:
```bash
source venv/bin/activate
```

- Windows:
```bash
.\venv\Scripts\activate
```

3. Instalar dependências e rodar
Com o ambiente ativado, instale os requisitos e execute o script:
```bash
pip install -r requirements.txt
python main.py
```

## 💻 Frontend (Next.js)

Primeiro, instale as dependências (caso ainda não tenha feito):

```bash
npm install
# ou
yarn
# ou
pnpm install
```

Em seguida, rode o servidor de desenvolvimento: 

```bash
npm run dev
# ou
yarn dev
# ou
pnpm dev
# ou
bun dev
```

